<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Agenda de Contactos</display-name>
    <description>Aplicación web para gestión de contactos con JSP y Servlets</description>

    <!--
        NOTA: Los servlets están registrados con @WebServlet en sus clases Java.
        NO se declaran aquí para evitar el conflicto de doble registro que
        causaba el error 500 en la versión original.
    -->

    <!-- Welcome file list -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Session configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- Error pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/error404.jsp</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/error500.jsp</location>
    </error-page>

    <!-- Character encoding filter -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>controladores.EncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Authentication filter -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>controladores.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/dashboard.jsp</url-pattern>
        <url-pattern>/contactos</url-pattern>
        <url-pattern>/usuarios</url-pattern>
        <url-pattern>/usuarios.jsp</url-pattern>
    </filter-mapping>

</web-app>
